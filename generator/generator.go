package generator

import "fmt"

// GenerateJSON will generate JSON from an image that can be parsed by
// arTTY to display in a terminal.
func GenerateJSON(filename string, name string) {
	// TODO generate JSON
	fmt.Println(filename)
	fmt.Println(name)
}
